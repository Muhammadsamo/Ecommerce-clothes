import { Link } from "react-router-dom";

export const Logo = () => {
  return (
    <div className="logo font-Monserrat uppercase lg:text-[32px] text-[25.2px] font-black text-black">
      <Link to="/">SHOP.CO</Link>
    </div>
  );
};
